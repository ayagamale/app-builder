# Local placeholders so the app boots before credentials are provided.
# Real values are delivered by the platform to /run/base44/app.env and override
# these (later env_file entry wins). Do not put real secrets here.
TOTALUM_VCAAS_API_KEY=
